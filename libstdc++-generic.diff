# HG changeset patch
# Parent 724438e38408b0d9d8a1926fe245a69f88503577
Use the generic implementation of libstdc++ primitives when we're on musl, not the glibc one.

diff -r 724438e38408 libstdc++-v3/configure.host
--- a/libstdc++-v3/configure.host	Fri Dec 25 15:05:43 2015 -0500
+++ b/libstdc++-v3/configure.host	Fri Dec 25 15:06:25 2015 -0500
@@ -243,6 +243,13 @@
     os_include_dir="os/bsd/freebsd"
     ;;
   gnu* | linux* | kfreebsd*-gnu | knetbsd*-gnu)
+    # check for musl by target
+    case "${host_os}" in
+      *-musl*)
+        os_include_dir="os/generic"
+        ;;
+      *)
+
     if [ "$uclibc" = "yes" ]; then
       os_include_dir="os/uclibc"
     elif [ "$bionic" = "yes" ]; then
@@ -251,6 +258,9 @@
       os_include_dir="os/gnu-linux"
     fi
     ;;
+
+    esac
+    ;;
   hpux*)
     os_include_dir="os/hpux"
     ;;
